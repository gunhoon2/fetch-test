[project]
name = "krxfetch"
version = "0.0.0"
authors = [
  { name="Gunhoon Lee", email="gunhoon@gmail.com" },
]
dependencies = [
    "requests",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
